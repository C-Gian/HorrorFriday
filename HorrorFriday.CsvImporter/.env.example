# Copy this file to .env and fill in your values.
# .env is gitignored — never commit it.

# PostgreSQL connection string
HF_DB=Host=localhost;Port=5432;Username=postgres;Password=porcodio_99;Database=horrorfriday

# OpenAI API key — needed for Mode [1] (embeddings) and Mode [4] (new records import)
HF_OPENAI_KEY=your_openai_api_key_here
